backend:
  name: github
  repo: kooku94/kooku94.github.io
  branch: main
media_folder: public/images
public_folder: /images
site_url: https://kooku94.github.io
base_url: https://api.github.com
local_backend: true
# publish_mode: editorial_workflow

collections:
  - name: 'meta'
    label: 'Meta'
    delete: false
    editor:
      preview: false
    files:
      - name: 'tags'
        label: 'Tags'
        file: 'meta/tags.yml'
        description: 'List of tags'
        fields:
          - name: tags
            label: Tags
            label_singular: 'Tag'
            widget: list
            fields:
              - label: 'Slug'
                name: 'slug'
                widget: 'string'
                hint: 'The part of a URL identifies the tag'
              - label: 'Display Name'
                name: 'name'
                widget: 'string'
                hint: 'Tag name for displaying on the site'

  - name: 'resume'
    label: 'Resume'
    folder: '_resume'
    extension: 'mdx'
    format: 'frontmatter'
    create: false
    delete: false
    slug: 'resume'
    identifier_field: slug
    summary: 'Resume'
    fields:
      - label: 'Body'
        name: 'body'
        widget: 'markdown'

  - name: 'posts'
    label: 'Posts'
    folder: '_posts'
    extension: 'mdx'
    format: 'frontmatter'
    create: true
    slug: '{{title}}'
    identifier_field: slug
    summary: '{{title}}'
    preview_path: posts/{{slug}}.html
    fields:
      - label: 'Title'
        name: 'title'
        widget: 'string'
      - label: 'Publish Date'
        name: 'date'
        widget: 'datetime'
        format: 'YYYY-MM-DD'
        dateFormat: 'YYYY-MM-DD'
        timeFormat: false
      - label: 'Cover'
        name: 'cover'
        widget: 'image'
        required: false
        default: '/images/sample-cover.png'
      - label: Tags
        label_singular: 'Tag'
        name: 'tags'
        widget: list
        summary: '{{fields.tag}}'
        field:
          label: Tag
          name: tag
          widget: relation
          collection: 'meta'
          file: 'tags'
          searchFields:
            - 'tags.*.name'
          displayFields:
            - 'tags.*.name'
          valueField: 'tags.*.slug'
      - label: 'Body'
        name: 'body'
        widget: 'markdown'
