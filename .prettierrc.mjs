/** @type {import('prettier').Options} */
const config = {
  singleQuote: true,
  semi: true,
  tabWidth: 2,
  useTabs: false,
  trailingComma: 'all',
  printWidth: 120,
  arrowParens: 'always',
};

export default config;
